spring:
  application:
    name: OAuth2

  security:
    oauth2:
      client:
        registration:
          github:
            client-id: Ov23liDdrKlbObKKMH8X
            client-secret: 160adcae3088867f4eca4a264dbef0f3ec197872
          auth0:
            client-id: client-id
            client-secret: secret
            scope: openid, offline_access
            authorization-grant-type: authorization_code
          personal:
            client-id: client
            client-secret: secret
            scope: offline_access
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"

        provider:
          auth0:
            issuer-uri: https://genesis-ai.us.auth0.com/
            authorization-uri: https://genesis-ai.us.auth0.com/authorize
            token-uri: https://genesis-ai.us.auth0.com/oauth/token
            user-info-uri: https://genesis-ai.us.auth0.com/userinfo
          personal:
            authorization-uri: http://localhost:9000/oauth2/authorize
            token-uri: http://localhost:9000/oauth2/token
            jwk-set-uri: http://localhost:9000/oauth2/jwks


#server:
#  servlet:
#    session:
#      cookie:
#        same-site: lax
#        secure: false   # true only if HTTPS

#  forward-headers-strategy: framework



logging:
  level:
    org.springframework.security: TRACE
    org.springframework.security.oauth2: TRACE